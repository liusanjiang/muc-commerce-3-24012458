# 第9天学生项目：机器学习零基础数据准备

## 运行方法

```bash
python -m pip install -r requirements.txt
python validate_day09_environment.py
jupyter lab
```

打开`notebooks/day09_ml_preparation_student.ipynb`。Notebook已经提供完整处理骨架，你只需完成少量关键填空、运行检查点并撰写解释。

## 学生信息

- 姓名：刘三江
- 学号：24012458
- 班级：3

## 用自己的话回答

- 什么是特征，什么是标签：特征是用于训练模型的数据维度，比如用户的年龄、消费金额、登录次数等，是模型用来学习的‘输入’。标签是我们希望模型预测的目标，比如‘是否流失’（1代表流失，0代表未流失），是模型的‘正确答案’。在代码中，X代表特征矩阵，y代表标签向量。
"""

- 为什么要保留测试集：测试集模拟的是模型从未见过的新用户数据，用于评估模型的真实泛化能力。如果只用训练集评估，模型可能‘过拟合’（死记硬背训练数据），但在面对新数据时表现很差。保留测试集能让我们在最终上线前，客观地看到模型在真实业务场景中的效果。
"""

- 为什么83%准确率仍可能没有用：因为客户流失数据中，未流失用户通常占绝大多数（比如80%以上）。如果模型全部预测‘未流失’，准确率就已经有80%了。83%的准确率只比随机猜测高一点点，说明模型没有真正识别出流失用户。此时要看‘召回率’（Recall）或‘F1分数’，而不是单纯看准确率。
"""

